<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SectorMapQuest.Views.MainPage"
    BackgroundColor="#121212"
    NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- ===== TOP BAR ===== -->
        <Grid>

            <!-- КНОПКА НАСТРОЕК -->
            <Label
                Text="⚙"
                FontSize="24"
                TextColor="White"
                HorizontalOptions="End"
                VerticalOptions="Start"
                Margin="16">        

                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnSettingsClicked" />
                </Label.GestureRecognizers>
            </Label>

        </Grid>


        <!-- ===== CONTENT ===== -->
        <ContentView Grid.Row="1" x:Name="PageHost" />

        <!-- ===== BOTTOM BAR ===== -->
        <Border Grid.Row="2"
                BackgroundColor="#1F1F1F"
                StrokeThickness="0"
                Padding="10"
                StrokeShape="RoundRectangle 24">

            <Grid ColumnDefinitions="*,*,*,*">

                <VerticalStackLayout Grid.Column="0" Spacing="4" HorizontalOptions="Center">

                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnStartClicked" />
                    </VerticalStackLayout.GestureRecognizers>

                    <Label x:Name="MapText"
                           Text="🗺"
                           FontSize="22"
                           HorizontalTextAlignment="Center" />
                    <Label Text="Map"
                           FontSize="12"
                           TextColor="Gray" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="1" Spacing="4" HorizontalOptions="Center">
                    <Label x:Name="FeedIcon" Text="📰" FontSize="22"/>
                    <Label x:Name="FeedText" Text="Feed" FontSize="12" TextColor="Gray"/>
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnFeedTapped"/>
                    </VerticalStackLayout.GestureRecognizers>
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="2" Spacing="4" HorizontalOptions="Center">
                    <Label x:Name="StatsIcon" Text="📊" FontSize="22"/>
                    <Label x:Name="StatsText" Text="Stats" FontSize="12" TextColor="Gray"/>
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnStatsTapped"/>
                    </VerticalStackLayout.GestureRecognizers>
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="3" Spacing="4" HorizontalOptions="Center">
                    <Label x:Name="ProfileIcon" Text="👤" FontSize="22"/>
                    <Label x:Name="ProfileText" Text="Profile" FontSize="12" TextColor="Gray"/>
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnProfileTapped"/>
                    </VerticalStackLayout.GestureRecognizers>
                </VerticalStackLayout>

            </Grid>
        </Border>

    </Grid>
</ContentPage>